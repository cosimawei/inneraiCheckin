# InnerAI 自动签到 - 完整部署流程

## 📋 准备工作

### 1. 确认你有以下信息
- ✅ GitHub 账号
- ✅ InnerAI 账号的 Session Cookie
- ✅ InnerAI 账号的 API User ID

---

## 🚀 第一步：创建 GitHub 私有仓库

1. 登录 GitHub
2. 点击右上角 `+` → `New repository`
3. 填写信息：
   - **Repository name**: `innerai-check-in`
   - **Description**: `InnerAI 自动签到工具`
   - **⚠️ 重要：选择 Private（私有仓库）**
4. 点击 `Create repository`

---

## 📤 第二步：上传代码到 GitHub

### 方式 A：使用一键部署脚本（推荐）

在项目目录右键，选择"在终端中打开"，运行：

```powershell
.\deploy-to-github.ps1
```

按照提示输入你的 GitHub 仓库地址，例如：
```
https://github.com/你的用户名/innerai-check-in.git
```

### 方式 B：手动部署

```bash
cd E:\Tools\innerai-check-in

# 初始化 Git（如果还没初始化）
git init

# 添加所有文件
git add .

# 创建提交
git commit -m "Initial commit: InnerAI 自动签到"

# 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/innerai-check-in.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**注意**：如果提示需要登录，使用 GitHub Personal Access Token。

---

## 🔐 第三步：配置 GitHub Secrets

### 3.1 创建 production 环境

1. 在 GitHub 仓库页面，点击 `Settings`
2. 左侧菜单找到 `Environments`
3. 点击 `New environment`
4. 输入名称：`production`
5. 点击 `Configure environment`

### 3.2 添加账号配置

1. 在 production 环境页面，点击 `Add environment secret`
2. 填写：
   - **Name**: `ANYROUTER_ACCOUNTS`
   - **Value**: 你的账号配置（见下方）

#### 单账号配置示例：
```json
[{"name":"主账号","provider":"innerai","cookies":{"session":"你的session值"},"api_user":"你的api_user值"}]
```

#### 多账号配置示例：
```json
[{"name":"主账号","provider":"innerai","cookies":{"session":"session1"},"api_user":"10"},{"name":"备用账号","provider":"innerai","cookies":{"session":"session2"},"api_user":"20"}]
```

**⚠️ 重要**：
- JSON 必须是**单行格式**，不能换行
- 将 `你的session值` 替换为实际的 session cookie
- 将 `你的api_user值` 替换为实际的 API User ID

3. 点击 `Add secret` 保存

### 3.3 添加 Provider 配置（可选）

1. 继续点击 `Add environment secret`
2. 填写：
   - **Name**: `PROVIDERS`
   - **Value**: `{"innerai":{"domain":"https://innerai.net"}}`
3. 点击 `Add secret` 保存

---

## ⚙️ 第四步：启用 GitHub Actions

1. 在仓库页面点击 `Actions` 标签
2. 如果提示启用，点击 `I understand my workflows, go ahead and enable them`
3. 找到 `InnerAI 自动签到` workflow
4. 点击 `Enable workflow`（如果需要）

---

## 🧪 第五步：测试运行

1. 在 `Actions` 页面
2. 点击左侧的 `InnerAI 自动签到`
3. 点击右侧的 `Run workflow` 按钮
4. 选择 `main` 分支
5. 点击绿色的 `Run workflow` 按钮
6. 等待运行完成（约 1-2 分钟）
7. 点击运行记录查看详细日志

### 成功标志：
```
✅ InnerAI账号: Sign-in successful! Balance: $XXX.XX
✅ All accounts check-in successful!
```

---

## 🔄 如何添加/修改账号

### 方法 1：直接修改 GitHub Secrets

1. 进入 `Settings → Environments → production`
2. 找到 `ANYROUTER_ACCOUNTS`
3. 点击右侧的编辑图标（铅笔）
4. 修改 JSON 配置
5. 保存

### 方法 2：使用网页生成配置

1. 打开本地的 `index.html` 网页
2. 添加/修改账号信息
3. 点击"生成配置"
4. 复制生成的 JSON
5. 粘贴到 GitHub Secrets 中

---

## ⏰ 自动运行时间

配置完成后，GitHub Actions 会：
- **每 6 小时自动运行一次**
- 运行时间：北京时间 0点、6点、12点、18点（可能延迟 5-15 分钟）
- 你也可以随时手动触发运行

---

## 📊 查看运行历史

1. 进入 `Actions` 页面
2. 查看所有运行记录
3. 点击任意记录查看详细日志
4. 可以看到：
   - 签到是否成功
   - 当前余额
   - 已使用额度
   - 错误信息（如果有）

---

## 🔔 配置通知（可选）

如果想收到签到通知，在 production 环境中添加以下 Secrets：

### 钉钉机器人
- **Name**: `DINGDING_WEBHOOK`
- **Value**: `https://oapi.dingtalk.com/robot/send?access_token=xxx`

### 企业微信
- **Name**: `WEIXIN_WEBHOOK`
- **Value**: `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx`

### Telegram
- **Name**: `TELEGRAM_BOT_TOKEN`
- **Value**: `your_bot_token`
- **Name**: `TELEGRAM_CHAT_ID`
- **Value**: `your_chat_id`

---

## ⚠️ 重要提示

### 安全性
- ✅ 使用私有仓库
- ✅ 不要将 `.env` 文件提交到 Git
- ✅ 定期更换 Session Cookie（建议每月一次）
- ✅ 不要在公开场合分享你的配置

### Session 有效期
- Session Cookie 大约 1 个月有效
- 过期后会出现 401 错误
- 需要重新获取并更新 GitHub Secrets

### 免费额度
- GitHub 免费账户：每月 2000 分钟 Actions 时间
- 本项目每月约用 120-240 分钟
- 完全够用，无需担心

---

## 🔧 故障排除

### 问题 1：Actions 运行失败
- 检查 `ANYROUTER_ACCOUNTS` 配置格式
- 确认 JSON 是单行格式
- 查看运行日志中的错误信息

### 问题 2：401 错误
- Session cookie 已过期
- 重新获取 session 并更新 Secret

### 问题 3：找不到签到接口
- 检查 `PROVIDERS` 配置
- 确认 innerai.net 的 API 路径是否正确

### 问题 4：推送代码失败
- 检查 GitHub 仓库地址是否正确
- 确认是否有推送权限
- 可能需要配置 Personal Access Token

---

## 📞 需要帮助？

如果遇到问题：
1. 查看 Actions 运行日志
2. 检查配置是否正确
3. 确认 session 和 api_user 是否有效
4. 查看本地测试是否成功（运行 `uv run checkin.py`）
